import React from "react";

import JoinModal from "@/app/board/components/InitModal";

const Board = () => {
  // TODO - add layout which will be used in both /board & board/[id]
  return <JoinModal />;
};

export default Board;
